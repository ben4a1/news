spring:
  datasource:
    username: test
    password: test
  jpa:
    properties:
      hibernate:
        hbm2ddl.auto: validate
        show_sql: true
        format_sql: true

logging:
  level:
    org.hibernate.type: trace